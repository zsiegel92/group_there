On `/events/[id]` we can edit the address of an event (if we are an admin) and can create/update our attendance address.

We want

- The NextJS `after` function should be used to kick off ensuring all pairwise ...

scripts/shell/test_google_routes_api.sh
scripts/shell/test_google_routes_matrix_api.sh
